{% extends "FormationAppBundle::adminLayout.html.twig" %}


{% block content1 %}

    <div class="widget box">
        <div class="widget-header">
            <h4><i class="icon-reorder"></i> Derniers messages reçus </h4>
            <div class="toolbar no-padding">
                <div class="btn-group">
                    <span class="btn btn-xs widget-collapse"><i class="icon-angle-down"></i></span>
                </div>
            </div>
        </div>
        <div class="widget-content no-padding">
            <table class="table table-striped table-bordered table-hover table-checkable table-responsive datatable">
                <thead>
                <tr>

                    <th data-class="expand">Nom et prénom</th>
                    <th>Email</th>
                    <th>Sujet</th>
                    <th>Message</th>

                </tr>
                </thead>
                <tbody>
                {% for contact in contacts %}
                    <tr>

                        <td class="align-center">{{ contact.name }}</td>
                        <td class="align-center">{{ contact.email }}</td>
                        <td class="align-center">{{ contact.subject }}</td>
                        <td class="align-center">{{ contact.message }}</td>

                    </tr>
                {% endfor %}

                </tbody>
            </table>
        </div>
    </div>

{% endblock %}